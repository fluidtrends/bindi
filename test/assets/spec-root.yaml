api:
  name: test
  settings:
    port: 9000
    root: /api
    awsConfigFile: config.json
  email:
    from: test@test.test
  database:
    name: test
    host: localhost
  routes:
    - name: tests
      path: /tests/:id
